import 'package:flutter/material.dart';

import '../widgets/add_money_body.dart';

class AddMoneyScreen extends StatelessWidget {
  const AddMoneyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AddMoneyBody(),
    );
  }
}
